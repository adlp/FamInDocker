check process bind with pidfile /var/run/named/named.pid
   group system
   start program = "/etc/init.d/bind9 start"
   stop program = "/etc/init.d/bind9 stop"
  # start program = "/usr/sbin/named -u bind"
  # stop program  = "/usr/sbin/rndc stop"
